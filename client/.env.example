# Where the Vite dev server proxies /api and /socket.io. Must match the API's PORT.
VITE_API_PROXY=http://localhost:5001

# Production API origin (no trailing slash). Add this in Vercel's environment
# variables after the Render service is live.
# VITE_API_URL=https://lifelink-api.onrender.com
