# Where the Vite dev server proxies /api and /socket.io. Must match the API's PORT.
VITE_API_PROXY=http://localhost:5001

# Only set this if the API is served from a different origin in production
# (i.e. not behind the same host). Leave unset to use same-origin requests.
# VITE_API_URL=https://api.example.com
